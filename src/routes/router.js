import {
    BrowserRouter as Router,
    Outlet,
    useNavigate,
    useRoutes,
    useLocation,
  } from 'react-router-dom';
  
  export {Router, Outlet, useNavigate, useRoutes,useLocation};
  